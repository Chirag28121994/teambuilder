spring.application.name=teambuilder

# MongoDB's connection settings
spring.data.mongodb.uri=mongodb+srv://player:adminPlayer@cluster0.6ddmu1h.mongodb.net/admin
spring.data.mongodb.database=teambuilder

# Hibernate and JPA configuration (no longer needed for MongoDB)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Logging configuration
logging.level.root=INFO
logging.level.com.sports=DEBUG
logging.file.name=logs/teambuilder.log
